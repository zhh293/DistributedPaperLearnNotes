package chatWithOthers;

public class LongForQuery {

    /*长轮询

    浏览器发出ajax请求，服务器端接收到请求后，会阻

            塞请求直到有数据或者超时才返回


            就是说如果客户端发送了一个请求，服务器不会直接返回，而是等服务器中有数据更新之后才会返回响应

            不会出现无意义的http请求


            但是也有弊端的*/


}
