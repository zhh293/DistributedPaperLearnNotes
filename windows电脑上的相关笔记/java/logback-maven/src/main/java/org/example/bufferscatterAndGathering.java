package org.example;

public class bufferscatterAndGathering {
    public static void main(String[] args) {
        /*Scattering:将数据写入到buffer时，可以采用buffer数组，依次写入[分散]

        Gathering:从buffer读取数据时，可以采用buffer数组，依次读*/

        //这次使用serversocketchannel和socketchannel


    }
}
