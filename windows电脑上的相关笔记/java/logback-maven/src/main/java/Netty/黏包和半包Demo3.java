package Netty;

public class 黏包和半包Demo3 {
    public static void main(String[] args) {

    }
}

/*

TCP 是 “流式协议”，它会把数据当作连续的字节流来传输，不自带消息边界。比如你发送 3 条消息 A、B、C，TCP 可能把它们合并成 ABC 一次发（黏包），也可能把 A 拆成 A1 和 A2 分两次发（半包）。接收方只能看到一串字节，无法直接判断哪里是一个消息的开始 / 结束。
具体哪些场景需要考虑？
只要你传输的是 “有明确逻辑边界的独立消息”，就必须处理黏包 / 半包。典型场景包括：
        1. 自定义协议的结构化消息
比如内部系统的通信协议，格式可能是 [类型][长度][内容]，每个消息代表一个独立指令（如 “查询用户”“提交订单”）。

例：游戏中玩家的操作指令（“移动”“攻击”），每个指令是一个独立消息，接收方必须准确拆分才能正确处理。
        2. RPC 框架的请求 / 响应
RPC（如 Dubbo、gRPC）中，一次调用的 “请求数据” 和 “响应数据” 是独立单元。

例：客户端发 {method: "getUser", params: {id: 1}}，服务端必须完整接收后才能处理，否则会解析错误。
        3. 即时通讯消息
聊天软件中，一条文本 / 图片消息是一个独立单元。

例：用户发送 “你好”，接收方需要把这两个字完整展示，而不是收到 “你” 之后先显示一半，再等 “好”。
        4. 数据库 / 缓存的命令交互
比如 MySQL 协议、Redis 协议，客户端发送的 “查询命令”（如 SELECT * FROM user）和服务端返回的 “结果集” 都是独立消息。

例：Redis 的 SET key value 命令，必须完整接收才能执行，否则会误判命令格式。







LengthFieldBasedFrameDecoder（LCT 解码器）通过长度字段来精确定位消息边界，从而完美解决半包问题。我用具体例子详细解释它的工作原理：
半包问题场景
假设我们的协议格式是：

plaintext
[长度字段(4字节)=100][数据(100字节)]

当发送这个 104 字节的完整消息时，由于网络波动，可能分两次到达：

第一次只收到 前 50 字节（半包）：
[长度字段(4字节)=100][数据(46字节)]
第二次收到 后 54 字节：
[数据(54字节)]

接收方需要将这两部分合并成完整消息。
LCT 解码器的处理流程
步骤 1：识别长度字段
解码器配置：

java
运行
new LengthFieldBasedFrameDecoder(
    1024,      // 最大帧长度
    0,         // lengthFieldOffset: 长度字段从第0字节开始
    4,         // lengthFieldLength: 长度字段占4字节
    0,         // lengthAdjustment: 无需调整（长度字段只包含数据）
    4          // initialBytesToStrip: 跳过长度字段
);

当第一次收到 50 字节时：

解码器从第 0 字节读取长度字段，得到值 100（表示数据长度）。
计算完整帧总长度 = 100（数据） + 0（调整） + 0（偏移） + 4（长度字段） = 104 字节。
发现当前数据只有 50 字节，不足 104 字节 → 解码器将这 50 字节放入内部缓冲区，等待剩余数据。
步骤 2：拼接剩余数据
当第二次收到 54 字节时：

解码器将这 54 字节追加到内部缓冲区，此时缓冲区总长度 = 50 + 54 = 104 字节。
发现缓冲区数据已满足总长度要求 → 解码器提取完整的 104 字节数据。
剥离前 4 字节（长度字段），将剩余的 100 字节数据 传递给下一个处理器。
关键机制解析
内部缓冲区：
解码器会缓存所有未完成的消息片段，直到凑够完整长度。
长度预判：
通过读取长度字段，提前知道需要等待多少数据才能组成完整消息。
零拷贝拼接：
Netty 使用 CompositeByteBuf 高效拼接多个片段，避免内存复制。
超时处理：
如果长时间收不到完整消息（可能网络故障），会触发 TooLongFrameException。




更多半包场景示例
场景 1：多次分片
完整消息分三次到达：

第一次：[长度字段(4)=100][数据(20)]（24 字节）
第二次：[数据(30)]（30 字节）
第三次：[数据(50)]（50 字节）

解码器处理：

第一次收到 24 字节 → 发现需要 104 字节，缓存 24 字节。
第二次收到 30 字节 → 缓存追加到 24+30=54 字节，仍不足 104 字节。
第三次收到 50 字节 → 缓存达到 54+50=104 字节，提取完整消息。
场景 2：黏包 + 半包混合
发送两条消息：

消息 1：[长度(4)=50][数据(50)]（54 字节）
消息 2：[长度(4)=30][数据(30)]（34 字节）

实际到达：

第一次：[消息1前30字节]（30 字节）
第二次：[消息1后24字节][消息2前10字节]（34 字节）
第三次：[消息2后24字节]（24 字节）

解码器处理：

第一次收到 30 字节 → 发现消息 1 需要 54 字节，缓存 30 字节。
第二次收到 34 字节 → 拼接出完整消息 1（30+24=54 字节），提取并传递；剩余 10 字节为消息 2 前缀，缓存。
第三次收到 24 字节 → 与之前的 10 字节拼接成完整消息 2（10+24=34 字节），提取并传递。



*/
