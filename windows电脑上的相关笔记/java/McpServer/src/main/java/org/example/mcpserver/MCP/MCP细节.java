package org.example.mcpserver.MCP;

public class MCP细节 {
}


/*
我说一下我的想法，你帮我补充一下，就是我认为MCP服务器是一个远程的工具，可以是python项目，springboot项目等等，当我部署到服务器上面的时候，你就可以去远程调用他了，而当前市面上的ai添加MCP服务器并归自己所用的时候其实需要遵守协议的，比如你先填入mcp服务器的地址和名字，点击添加mcp服务器，这时他会往你的服务器发送请求，用来拿到初始化的json字符串，然后建立长连接，成功之后ai就可以根据问题来给mcp服务器传入参数了，但是这建立长连接的过程我还是不懂具体分为哪些，我上面说的对吗
你的理解基本正确！MCP（Model Context Protocol）本质上是 AI 模型与外部工具（如你的部署的 Python/SpringBoot 服务）之间的标准化通信协议，核心作用是让 AI 能 “调用” 你的服务器能力。你的描述抓住住了核心流程，以下是补充和细化，尤其针对 “长连接建立过程” 的技术细节：
一、你的理解补充（正确部分）
MCP 服务器的本质：确实可以是任何语言开发的服务（Python/Java/Node.js 等），只要遵循 MCP 协议规范暴露接口，就能被 AI 客户端识别和调用。部署到云服务器后，AI 通过公网地址远程访问。
添加 MCP 服务器的核心流程：
填写地址（基础 URL）和名称 → AI 客户端发起初始化请求 → 获取配置信息 → 建立长连接 → 实时交互。
这一流程的核心是 **“协议共识”**：AI 和你的服务器必须按 MCP 规范 “说话”，否则无法通信（比如你之前遇到的 “settings 格式错误” 就是协议不匹配）。
二、长连接建立的具体步骤（技术细节）
长连接（通常用SSE或WebSocket实现，MCP 更常用 SSE）的作用是让 AI 和服务器能实时双向通信（比如 AI 发送工具调用参数，服务器返回执行结果），具体过程分 4 步：
        1. 协议握手：初始化请求（获取 settings）
AI 客户端向你的服务器发送GET 请求（如 http://你的服务器地址/api/mcp/settings），目的是：
        ① 确认服务器是否支持 MCP 协议（通过返回的protocolVersion字段匹配）；
        ② 获取服务器能力（如支持的工具列表tools: ["查天气", "算数学"]、是否需要认证等）。
服务器必须返回标准 JSON（如之前提到的protocolVersion、capabilities等字段），否则 AI 会判定 “协议不兼容”。
        2. 建立长连接（SSE 为例）
初始化通过后，AI 客户端发起SSE 连接请求（如 http://你的服务器地址/sse）。
SSE（Server-Sent Events）是一种 HTTP 长连接技术，特点是：
基于 HTTP 协议，无需额外端口（复用 80/443 或你配置的端口）；
服务器可主动向客户端推送数据（适合 AI 调用工具的场景：AI 发请求，服务器异步返回结果）；
连接建立后，HTTP 连接保持 “打开” 状态（而非一次性关闭），通过心跳包维持。
        3. 数据交互：工具调用的实时通信
当用户问 “今天北京天气如何？”，AI 判定需要调用你的 “查天气” 工具：
        ① AI 通过 SSE 连接向服务器发送工具调用指令（JSON 格式）：
json
{
    "action": "callTool",
        "tool": "查天气",
        "params": {"city": "北京"},
    "requestId": "唯一标识"  // 用于匹配请求和结果
}
② 你的服务器执行查询后，通过同一 SSE 连接推送结果：
json
{
    "requestId": "唯一标识",  // 对应AI的请求
        "status": "success",
        "result": {"temperature": "25℃", "condition": "晴"}
}
整个过程中，SSE 连接始终保持，无需重复建立（这就是 “长连接” 的意义：减少重复握手开销）。
        4. 连接维护与断开
心跳机制：若长时间无数据，服务器会定期发送空消息（如: ping\n\n），客户端收到后回应，确认连接存活。
断开重连：若网络波动导致连接中断，AI 客户端会自动重试（根据settings中的配置，如reconnectTimeout），直到重新建立连接。
三、关键补充：协议规范的核心作用
你提到 “需要遵守协议”，这是核心。MCP 协议的本质是定义 “AI 如何问” 和 “服务器如何答” 的规则：
比如 AI 必须用action: "callTool"表示调用工具，服务器必须用requestId关联请求和结果，否则双方无法理解彼此的消息。
这就像两个人打电话：必须说同一种语言（协议），才能沟通。
总结
你的理解是准确的！长连接建立的核心是 “先通过初始化请求确认协议兼容，再用 SSE 等技术保持 HTTP 连接打开，实现实时双向通信”。关键点在于：
服务器必须实现settings接口（握手）和 SSE 接口（长连接）；
消息格式严格遵循 MCP 规范（JSON 结构、字段定义）；
通过心跳和重连机制维持连接稳定性。
按这个逻辑，你的 MCP 服务器就能被 AI 稳定调用了。
数据交互：工具调用的实时通信
当用户问 “今天北京天气如何？”，AI 判定需要调用你的 “查天气” 工具：
        ① AI 通过 SSE 连接向服务器发送工具调用指令（JSON 格式）：
json
{
    "action": "callTool",
        "tool": "查天气",
        "params": {"city": "北京"},
    "requestId": "唯一标识"  // 用于匹配请求和结果
}
② 你的服务器执行查询后，通过同一 SSE 连接推送结果：
json
{
    "requestId": "唯一标识",  // 对应AI的请求
        "status": "success",
        "result": {"temperature": "25℃", "condition": "晴"}
}

SSE不是只能单向推送吗，这是怎么做到的
SSE 本身确实是服务器单向向客户端推送消息的技术（客户端发起连接后，只能被动接收服务器的推送）。但工具调用的 “双向交互”，是通过 “SSE（服务器→客户端） + 普通 HTTP 请求（客户端→服务器）” 组合实现的，并非仅靠 SSE 单一通道：
实际流程拆解（以 “查天气” 工具调用为例）：
客户端→服务器：发送工具调用请求AI 决定调用 “查天气” 工具时，不是通过 SSE 连接发送指令，而是通过一个普通的 HTTP POST 接口（比如服务器提前暴露的 /api/mcp/tool/call），将工具调用的 JSON（包含 action、tool、params、requestId 等）以 POST 请求的方式发送给服务器。
服务器处理请求服务器收到 POST 请求后，解析参数（如城市 “北京”），执行 “查天气” 的业务逻辑（比如调用第三方天气 API、查询数据库等）。
服务器→客户端：推送结果（通过 SSE）处理完成后，服务器通过已建立的 SSE 连接，将结果（包含对应的 requestId，确保与请求一一对应）推送给客户端（AI）。
这样，虽然 SSE 本身是 ** 单向（服务器→客户端）** 的，但通过 “普通 HTTP 请求（客户端→服务器） + SSE（服务器→客户端）” 的组合，就实现了 “AI 发指令 → 服务器处理 → 服务器推结果” 的双向交互效果。
简单总结：SSE 只负责 “服务器→AI” 的结果推送，而 “AI→服务器” 的指令发送是通过另一个 HTTP 接口完成的。*/


