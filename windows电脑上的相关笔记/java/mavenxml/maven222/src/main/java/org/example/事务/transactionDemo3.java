package org.example.事务;

public class transactionDemo3 {
    //事务管理员和协调员
    //spring事务角色
    /*事务角色

    事务管理员:发起事务方

            在Spring中通常指代业务层开启事务的方法

    事务协调员

            加入事务方

    在Spring中通常指代数据层方法，也可以是业务层方法*/
    /*图中展示了 Spring 事务传播行为（以默认的REQUIRED传播行为为例）的典型应用场景，具体描述如下：

    事务开启者（外层方法）：
    服务层的transfer方法标注了@Transactional，开启事务 T。该方法负责调用 DAO 层的扣钱（outMoney）和加钱（inMoney）操作，构成转账的业务逻辑。
    事务参与者（内层方法）：
    AccountDao的outMoney和inMoney方法未显式标注@Transactional，但由于 Spring 事务传播行为默认是REQUIRED（即：若当前存在事务，则加入该事务；若不存在，则创建新事务），它们会自动加入外层transfer方法开启的事务 T。
    因此，outMoney（扣钱）、inMoney（加钱）与transfer方法共享同一个事务上下文，三者的数据库操作（更新）在事务 T 中执行。
    事务原子性保证：
    当transfer方法执行时，若outMoney成功但inMoney失败（或反之），整个事务 T 会回滚，确保扣钱和加钱操作要么全部成功，要么全部失败（如转账时，不会出现 “钱扣了没到账” 或 “没扣钱却到账” 的情况）。这体现了事务的原子性，通过传播行为实现了多层方法对同一事务的协作。
    关键逻辑：
    传播行为REQUIRED：内层方法（DAO 操作）依赖外层事务（服务方法），自动加入已有事务，避免创建新事务（减少事务开销，且保证业务逻辑的原子性）。
    事务边界：由外层@Transactional方法定义事务范围，内层方法通过传播行为融入该范围，共同维护数据一致性。
    总结：
    图中通过@Transactional注解和默认传播行为，实现了外层方法开启事务，内层方法加入事务的协作模式，确保转账操作的原子性，是 Spring 事务管理中 “事务传播” 特性的典型应用（通过REQUIRED传播行为，将多个数据库操作纳入同一事务，保证数据完整性）。*/





}
