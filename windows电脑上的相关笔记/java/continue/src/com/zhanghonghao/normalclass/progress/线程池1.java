package com.zhanghonghao.normalclass.progress;

import java.util.Collection;
import java.util.Iterator;
import java.util.concurrent.*;

public class 线程池1 {
    public static void main(String[] args) {
        ThreadPoolExecutor threadPoolExecutor=new ThreadPoolExecutor(
                3,//核心线程数量
                6,//最大线程数量   最大数量一定要大于核心线程数量
                60,//临时线程最大存活时间的值
                TimeUnit.SECONDS,//单位
                new ArrayBlockingQueue<>(3),//任务队列
                Executors.defaultThreadFactory(),//创建线程工厂，负责创建临时线程
                new ThreadPoolExecutor.AbortPolicy()//任务的拒绝策略
        );
//        划重点：什么时候创建内部类，当一个类依赖另一个类存在时，而他单独存在没有意义时，就可以创建某个类的内部类
//        假设你在写一个 “汽车” 类，汽车里有 “发动机” 类。发动机和汽车的关系非常紧密，发动机需要访问汽车的私有数据（比如油箱油量），
//        而且发动机单独拿出来没什么用。这时候，把 “发动机” 作为 “汽车” 的内部类，就像把发动机 “封装” 在汽车里，既方便又合理。
//如果一个类只在另一个类里有用，或者需要紧密结合另一个类的数据，就把它做成内部类
        //为什么AbortPolicy是内部类
        /*二、为什么设计成内部类？
        1. 紧密关联线程池逻辑，封装专用功能
        AbortPolicy 是线程池 “拒绝策略” 的具体实现之一。拒绝策略的作用是：当线程池无法处理新任务时（如任务队列已满、线程数达到上限），决定如何处理该任务。

        这类策略是线程池的 “专属功能”，与线程池的状态（如核心线程数、最大线程数、任务队列）紧密相关，因此将其作为内部类，
        封装在 ThreadPoolExecutor 内部，符合 “高内聚” 的设计原则。*/


//        假如你开了一家工厂，每天有很多订单（任务）需要处理。
        /*聪明的做法：养一批固定员工（核心线程），订单多时加临时工（最大线程），订单少了让临时工先休息（超时销毁），排队的订单放仓库（阻塞队列），实在忙不过来就按规则拒绝（拒绝策略）。
        这就是 线程池 的核心思路：复用线程、控制数量、管理任务队列*/
       /* 三、自定义线程池的 7 个核心参数（工厂招工规则）
        先看构造方法（别被参数吓到，我逐个翻译）：

        java
        new ThreadPoolExecutor(
                corePoolSize,       // 核心员工数（固定长期在岗，不会被开除）
                maximumPoolSize,    // 最大员工数（核心+临时工，临时工忙完会休息一段时间后离开）
                keepAliveTime,      // 临时工的“空闲超时时间”（没活干多久后被开除）
                unit,               // 时间单位（比如秒、毫秒）
                workQueue,          // 任务仓库（订单排队的地方）
                threadFactory,      // 员工招聘处（自定义员工名字、优先级等）
                handler            // 拒绝策略（订单太多，仓库满了、员工也忙不过来时怎么处理）
        );
        1. 核心员工数（corePoolSize）
        作用：工厂里至少保留的员工数，即使没活干也不失业（线程不会销毁）。
        2. 最大员工数（maximumPoolSize）
作用：工厂最多能招多少员工（核心 + 临时工），防止员工太多挤爆工厂。
 临时工超时时间（keepAliveTime + unit）
作用：临时工没活干时，等多久就被开除（释放资源）。比如设 30 秒，30 秒没活就走。
场景：适合任务量波动大的场景（比如电商促销时临时加人，过后自动减少）。
 任务仓库（workQueue）
作用：订单太多，员工忙不过来，先放仓库排队。
常见类型：
有界队列（ArrayBlockingQueue）：比如设定最多存 1000 个订单，满了就触发拒绝策略（推荐，避免内存爆炸）。
5. 员工招聘处（threadFactory）
作用：自定义员工的 “属性”，比如给线程起名字（“工厂 - 员工 - 1”“工厂 - 员工 - 2”），
方便排查问题（日志里看到名字就知道是哪个线程池的）。

6. 拒绝策略（handler）
作用：订单太多，仓库满了，员工也全忙，怎么办？
4 种策略：
直接拒绝（AbortPolicy）：抛异常（“老子不干了！”，默认策略，适合必须处理任务的场景）。
让老板自己干（CallerRunsPolicy）：把任务扔回给调用者线程处理（比如主线程自己去执行任务，适合不想丢失任务的场景）。
丢弃最老的订单（DiscardOldestPolicy）：扔掉仓库里最久的那个订单，处理新的（适合允许丢任务，但要保证最新任务优先）。
默默丢弃（DiscardPolicy）：直接扔掉任务，不报错（适合任务不重要，丢了就丢了）。



*/

        /*是的，当你在创建 ThreadPoolExecutor 时传入拒绝策略（包括预定义的内部类策略或自定义策略），
        当任务过多且无法被线程池处理时，
        底层会自动触发并执行拒绝策略中的 rejectedExecution 方法。以下是具体原理和流程：*/

    }
}
