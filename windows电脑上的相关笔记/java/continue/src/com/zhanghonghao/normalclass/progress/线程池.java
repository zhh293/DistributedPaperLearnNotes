package com.zhanghonghao.normalclass.progress;

import java.lang.reflect.Constructor;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class 线程池 {
    public static void main(String[] args) {
       /*ExecutorService executorService = Executors.newCachedThreadPool();
       //给线程池提交任务
        executorService.submit(new progress1());
        executorService.submit(new progress1());
        executorService.submit(new progress1());
        executorService.submit(new progress1());
        //销毁线程池
        executorService.shutdown();*/
        /*创建一个池子，池子中是空的

        提交任务时，池子会创建新的线程对象，任务执行完毕，线程归还给池子

        下回再次提交任务时，不需要创建新的线程，直接复用已有的线程即可

        但是如果提交任务时，池子中没有空闲线程，也无法创建新的线程，

        任务就会排队等待*/
        //自定义线程池；；；核心线程和临时线程






    /*    你可以把它想象成一个 “餐厅后厨的厨师团队”：

        每个 “线程” 就像一个厨师，专门负责处理一个 “任务”（比如炒一道菜）。
        但如果每次来一个订单（任务）就临时招一个厨师，做完就解雇，那太麻烦了：招人和解雇需要时间（创建 / 销毁线程有开销），而且厨师太多时厨房可能挤不下（线程太多占用资源）。
        线程池就相当于餐厅提前雇了一批固定的厨师（核心线程），让他们一直在厨房待命。订单来了直接派给空闲的厨师，没订单时厨师也不闲着（等着下一个任务）。如果订单突然暴增，固定厨师忙不过来，就临时扩招一批兼职厨师（临时线程），但最多不能超过厨房能容纳的人数（最大线程数）。要是订单多到连兼职厨师都忙不过来，就把订单先记在本子上排队（工作队列），或者告诉客人 “暂时做不了”（拒绝策略）。
        线程池的核心作用：解决 3 个痛点
        避免重复 “招人和解雇” 的浪费
        如果不使用线程池，每次有任务都要新建一个线程，任务结束就销毁线程。就像每次炒个菜雇个厨师，炒完就赶走，下次再雇新的。频繁创建和销毁线程会消耗大量时间和资源（比如 CPU 调度、内存分配），效率很低。
        线程池里的线程可以重复使用，任务来了直接分配给空闲线程，省去了 “新建 - 销毁” 的开销。
        控制 “厨师数量”，防止厨房爆炸
        如果任由任务无限创建线程，就像餐厅不管来多少订单都疯狂招厨师，最后厨房挤得水泄不通，所有人都没法干活（线程过多导致系统资源耗尽，甚至卡死）。
        线程池通过设置 “最大线程数”，强制限制同时工作的线程数量，保证系统稳定。
        管理 “排队的订单”，避免手忙脚乱
        当任务太多，现有厨师（线程）忙不过来时，线程池会把任务先放在 “队列” 里排队，而不是让任务乱成一锅粥。就像餐厅用菜单记录待处理的订单，按顺序处理，避免混乱。
        线程池的 “四大组件”，详细拆解
        1. 核心线程（Core Pool Size）
        固定在岗的 “正式厨师”，数量是你提前设定好的。哪怕他们暂时没活干（空闲状态），也不会被解雇，一直留在池子里等着接任务。
        比如你设定核心线程数为 5，那么线程池启动后，会先创建 5 个线程，随时待命。
        2. 工作队列（Work Queue）
        任务排队的 “本子”。当核心线程都在忙时，新任务会先放进队列里等着，而不是立刻创建新线程。
        队列有不同的 “排队规则”，比如：
        先进先出（FIFO）：先来的任务先处理（像普通餐厅按订单时间顺序做菜）。
        优先级队列：重要的任务先处理（比如 VIP 订单优先）。
        无界队列：理论上可以无限排队（但可能导致内存溢出，慎用）。
        3. 最大线程数（Maximum Pool Size）
        线程池能容纳的最大 “厨师数量”，包括核心线程和临时扩招的 “兼职厨师”。
        当队列里的任务也满了，线程池会创建临时线程（直到达到最大线程数）来处理更多任务。比如核心线程 5 个，最大线程 10 个，当队列满了，就再招 5 个兼职厨师，总共 10 个一起干活。
        4. 拒绝策略（Rejected Execution Handler）
        任务太多时的 “应对方案”。当核心线程 + 临时线程都忙不过来，队列也满了，就需要拒绝新任务，常见策略有：
        直接拒绝（AbortPolicy）：抛异常，告诉调用者 “处理不了”（比如餐厅对客人说 “今天不接单了”）。
        让调用者自己处理（CallerRunsPolicy）：把任务丢回给调用线程处理（比如客人自己下厨炒菜，有点离谱但能应急）。
        丢弃最老的任务（DiscardOldestPolicy）：扔掉队列里最久的任务，处理新任务（比如取消最早的订单，先做新订单，可能让老客户不满）。
        静默丢弃（DiscardPolicy）：直接忽略新任务，不报错也不处理（比较 “佛系”，可能丢数据）。*/

    }
}
