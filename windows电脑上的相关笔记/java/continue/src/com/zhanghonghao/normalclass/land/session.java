package com.zhanghonghao.normalclass.land;

import javax.net.ssl.SSLSession;

public class session {
    SSLSession session;
    /*Session
角色：Session 如同服务器为每个客户端创建的一个专属 “房间”。服务器会给每个客户端分配一个唯一的 “房间号”（Session ID），客户端通过这个 “房间号” 来访问自己的 “房间”。
作用
保持用户状态：在用户和网站交互的过程中，网站需要知道用户处于什么状态，比如用户是否已登录、购物车中有哪些商品等。服务器可以把这些信息存到用户对应的 Session 中，方便随时获取和更新。
确保数据安全：由于 Session 数据存储在服务器端，相比存储在客户端的 Cookie 更安全。敏感信息（如用户的账号余额、订单详情等）存放在 Session 中，能降低信息泄露的风险。
支持多页面交互：当用户在网站的多个页面间切换时，服务器可以通过 Session 保持用户的上下文信息，确保用户操作的连贯性。
在 Web 开发里，最初是把用户信息存在 Cookie 中，不过由于 Cookie 存储在客户端，容易遭受攻击，安全性较低，因此引入了 Session 机制。下面为你详细介绍信息在 Session 中的体现方式。
1. Session 的基本原理
当用户首次访问服务器时，服务器会为该用户创建一个唯一的 Session 对象，同时生成一个对应的 Session ID。这个 Session ID 会以 Cookie 的形式发送给客户端，
客户端后续每次请求都会携带这个 Session ID。服务器依据 Session ID 找到对应的 Session 对象，从而获取和管理该用户的相关信息。
Session 信息通常存储在服务器的内存、文件系统或者数据库中。
内存存储：这是最常见的存储方式，将 Session 信息存储在服务器的内存中，读写速度快，但服务器重启后 Session 信息会丢失。
文件系统存储：把 Session 信息存储在文件中，适合数据量较大且需要持久化存储的场景，但读写速度相对较慢。
数据库存储：使用数据库（如 MySQL、Redis 等）来存储 Session 信息，可实现数据的持久化和分布式存储，适合大规模应用场景。
总之，信息在 Session 中以键值对的形式存储，通过唯一的 Session ID 进行关联，服务器能够依据 Session ID 对用户的相关信息进行管理和维护。

相当于用户登陆过一次后，后端服务器会把用户信息存储在一个新创建的session数据中，并返回一个sessionid，
以后用户再次登录时，会给后端服务器传输sessionid，服务器通过sessionid查找对应的session对象，如果有，则直接放行，是吗
大体上是这样，不过为了更清晰地理解，下面详细解释一下这个过程：
首次登录阶段
用户发起登录请求：用户在前端页面输入账号和密码，点击登录按钮，前端将这些信息发送给后端服务器。
后端验证用户信息：后端服务器接收到登录请求后，会将用户输入的账号和密码与存储在数据库中的数据进行比对。如果匹配成功，说明用户身份合法。
创建 Session 对象：验证通过后，服务器会为该用户创建一个新的 Session 对象。这个 Session 对象就像是一个容器，用于存储与该用户相关的各种信息，比如用户名、用户权限、用户偏好设置等。
生成 Session ID：服务器会为这个新创建的 Session 对象生成一个唯一的标识符，即 Session ID。这个 ID 就像一把钥匙，用来唯一标识这个 Session 对象。
返回 Session ID 给客户端：服务器将生成的 Session ID 通过响应发送给客户端。通常情况下，这个 Session ID 会以 Cookie 的形式存储在客户端的浏览器中。
后续请求阶段
客户端携带 Session ID 发起请求：当用户后续再次访问该网站的其他页面或者发起其他请求时，客户端的浏览器会自动在请求头中携带之前存储的 Session ID 发送给后端服务器。
服务器查找对应的 Session 对象：后端服务器接收到请求后，会从请求头中提取出 Session ID，然后根据这个 ID 在服务器端存储的 Session 列表中查找对应的 Session 对象。
验证 Session 有效性：
若找到对应的 Session 对象：说明用户之前已经成功登录，并且当前的 Session 仍然有效。服务器会根据 Session 中存储的用户信息进行相应的处理，例如根据用户权限返回不同的页面内容，然后直接放行该请求，响应用户的操作。
若未找到对应的 Session 对象：可能是因为 Session 已经过期（比如超过了预设的有效期）、被手动销毁（例如用户点击了注销按钮）或者 Session ID 无效（可能是被篡改或者伪造）。在这种情况下，服务器会判定用户未登录，可能会重定向用户到登录页面，要求用户重新登录。
注意事项*/
}
